# Vercel – Consumer & Vendor apps
# Add in Vercel Dashboard → Project Settings → Environment Variables
# Use for both gatherly-consumer and gatherly-vendor projects
# Node 18 is set via engines.node in package.json (avoids pnpm ERR_INVALID_THIS)

# API base URL (Render API URL after deploy)
NEXT_PUBLIC_API_URL="https://gatherly-api.onrender.com"
