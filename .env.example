# Database (Docker Postgres – used by default)
DATABASE_URL="postgresql://gatherly:gatherly_dev@localhost:5432/gatherly"

# Neon (for db:migrate:neon / db:migrate:all – get from Neon dashboard)
# DATABASE_URL_NEON="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# JWT
JWT_SECRET="your-secret-key-change-in-production"
JWT_REFRESH_SECRET="your-refresh-secret-change-in-production"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="7d"

# API
API_PORT=3001
API_URL="http://localhost:3001"

# Consumer app (Next.js) – API base URL for fetch calls
# For mobile testing on same Wi‑Fi: set to your machine's IP in apps/consumer/.env.local
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Mobile testing: CORS origin for consumer when accessed via IP (e.g. http://192.168.100.242:3000)
# CONSUMER_URL_MOBILE="http://<your-ip>:3000"

# Cloudinary (image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Frontend (for CORS)
CONSUMER_URL="http://localhost:3000"
VENDOR_URL="http://localhost:3002"
ADMIN_URL="http://localhost:3003"
