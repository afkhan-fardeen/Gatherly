# =============================================================================
# Gatherly – Environment Variables
# =============================================================================
# API: Copy to .env at repo root (and optionally packages/api/.env for overrides).
# Consumer/Vendor: Create apps/consumer/.env.local with NEXT_PUBLIC_API_URL for
#   local dev. Production: set in Vercel dashboard per project.
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Dev: Docker Postgres (default)
DATABASE_URL="postgresql://gatherly:gatherly_dev@localhost:5432/gatherly"

# Prod / Neon: use connection pooler URL from Neon dashboard
# DATABASE_URL="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Optional: for db:migrate:all (migrates both local + Neon)
# DATABASE_URL_NEON="postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# -----------------------------------------------------------------------------
# JWT (API auth)
# -----------------------------------------------------------------------------
# Dev: any string; Prod: use strong random secret (e.g. openssl rand -base64 32)
JWT_SECRET="your-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Optional (if using refresh tokens)
# JWT_REFRESH_SECRET="your-refresh-secret"
# JWT_REFRESH_EXPIRES_IN="7d"

# -----------------------------------------------------------------------------
# API
# -----------------------------------------------------------------------------
API_PORT=3001
# Dev: localhost; Prod (Render): set in Render dashboard
API_URL="http://localhost:3001"

# -----------------------------------------------------------------------------
# Frontend apps (consumer, vendor)
# -----------------------------------------------------------------------------
# Dev: localhost or your machine IP for mobile testing on same Wi‑Fi
# Prod: set in Vercel dashboard per project (no quotes, no trailing slash)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# -----------------------------------------------------------------------------
# CORS (API allows these origins)
# -----------------------------------------------------------------------------
# Dev
CONSUMER_URL="http://localhost:3000"
VENDOR_URL="http://localhost:3002"
ADMIN_URL="http://localhost:3003"

# Prod: match your Vercel deployment URLs
# CONSUMER_URL="https://gatherly-consumer.vercel.app"
# VENDOR_URL="https://gatherly-vendor.vercel.app"

# Mobile testing: CORS when consumer accessed via IP (e.g. http://192.168.1.100:3000)
# CONSUMER_URL_MOBILE="http://<your-ip>:3000"

# -----------------------------------------------------------------------------
# Cloudinary (image uploads)
# -----------------------------------------------------------------------------
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
